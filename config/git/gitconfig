[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    dark = true
    line-numbers = true
    side-by-side = true

[merge]
    conflictStyle = zdiff3

[init]
    defaultBranch = main

[gpg]
    format = ssh

[user]
    signingKey = ~/.ssh/id_ed25519.pub

[commit]
    gpgSign = true
